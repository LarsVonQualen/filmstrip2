{
  "rules": {
    "votes": {
      "$imdbId": {
        ".read": "auth != null",

        "$user_id": {
          ".write": "auth != null && auth.uid === $user_id"
        }
      }
    },
    "collections": {
      "$user_id": {
        ".read": "auth != null && auth.uid === $user_id",
        ".write": "auth != null && auth.uid === $user_id"
      }
    }
  }
}
